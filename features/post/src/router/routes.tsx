import React from 'react'
import { type RouteObject } from '@rayseinc-core/types'
import { Closing } from '../screens'

export const routes: RouteObject[] = [
  {
    path: '/post',
    element: <Closing />
  }
]
